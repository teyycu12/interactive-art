# PersonaFlow 基底風格標準

參考檔案：[`docs/style_reference/base_character.png`](./docs/style_reference/base_character.png)
機器可讀版本：[`backend/style_base.py`](./backend/style_base.py)

這份文件定義的是**風格語言**，不是某一個角色。高矮胖瘦等個體差異由生成
prompt 攜帶，本文件的任何數值都不隨體型改變。

> [!IMPORTANT]
> **2026-08 架構轉向：本文件的判定方向已反轉，量測值本身仍然有效。**
>
> 這份標準原本是為「固定 3D 渲染器 + AI 只畫 UV 貼圖」寫的，所以把明暗當成
> **貼圖不得超過的預算**（下方第 1、2 節）。該路線已整條移除。
>
> 現在 `full_character` 由模型一次畫出整隻角色，**輸出即最終畫面，沒有渲染器
> 會再加上任何明暗**。因此下表量到的 0.08–0.24 垂直漸層從「貼圖的上限」
> 變成「**生成結果應該達到的目標**」。
>
> 同理，第 1 節的「貼圖方向性梯度上限 0.06」已不適用；`style_normalizer`
> 的用途從「扣掉多餘打光」改為「量測明暗是否收斂到參考值」。
>
> 第 3 節之後的比例、材質、線寬、配色量測不受影響，可直接沿用。
>
> **2026-08 畫風定案：光澤 3D 渲染感**（本文件量測的方向）。理由是角色未來要做成
> 會動的版本，材質與打光必須跨影格一致，而一致的材質定義正是這份標準提供的東西。
> `garment_gen.py` 的 ART STYLE GUIDELINES 已改寫成與本文件一致，並由
> `backend/tests/test_prompt_style_agreement.py` 鎖住，避免兩邊再度分歧。

所有數值都是從參考圖量出來的，不是目測估的。參考圖主體為 303×563 px，
因此所有位置都以主體外框的相對比例表示，日後換解析度重算仍成立。

量測信心標記：

| 標記 | 意義 |
|---|---|
| `measured` | 可直接從參考圖讀出 |
| `occluded` | 參考圖遮住了該特徵，只能當下界，**不具權威性** |
| `ordinal` | 參考圖能可靠決定排序，絕對值仍需視覺校正 |

---

## 1. 明暗的量測基準（判定方向已反轉，見上方說明）

參考圖上所有明暗——軀幹的柔和漸層、手臂的圓柱過渡、頭髮高光、鞋下接觸陰影
——**全部來自 3D 渲染**。而球衣的 V 領滾邊、`ABLE` 字樣、`9` 號碼、肩章、
牛仔褲縫線與磨白——**全部是平貼在表面的印刷**。兩者從不混在一起。

量到的數字讓這件事變成硬性預算：

| 部位 | 渲染的上下向漸層 | 渲染的左右向漸層 |
|---|---:|---:|
| 皮膚 | 0.212 | 0.007 |
| 牛仔 | 0.241 | 0.014 |
| 球衣 | 0.077 | 0.011 |
| 鞋 | 0.196 | 0.031 |

渲染本身就提供了 0.08–0.24 的垂直明暗，而且**左右幾乎為零**（主光是正面光，
不是側光）。所以貼圖裡任何方向性明暗都是疊加上去的。

> **貼圖方向性梯度上限：0.06**

（此上限屬於已移除的貼圖路線，保留作為量測方法的紀錄。在 `full_character`
下應改為：生成結果的垂直漸層應**接近**上表數值，而非低於某個上限。）

## 2. 材質風化合法，方向性打光非法

參考圖的牛仔褲有大量磨白與髒污，這是**材質風化**，是「服裝精細度逼近業界」
的主要來源之一，必須保留。現行 validator 只要量到亮度梯度就判 `baked_lighting`，
會把這種正確細節一起殺掉。

| 貼圖內允許 | 貼圖內禁止 |
|---|---|
| 布料織紋 | 方向性主光 |
| 牛仔磨白 | 環境遮蔽 (AO) |
| 磨損與做舊 | 投射陰影 |
| 縫線與接縫 | 倒角明暗 |
| 印刷墨線邊緣 | 輪廓光 |
| 材質髒污 | 暗角 |
| | 服裝外輪廓 |
| | 服裝以外的背景 |

**判別依據：風化在整個 panel 上沒有一致的光源方向。**

## 3. 粗糙度不是單一值

參考圖各材質的高光結構差異很大。`highlight_compactness` 是高光面積除以其自身
外框：值越低代表高光是細銳的條狀（越亮面），越高代表柔和大面積衰減（越霧面）。

| 材質 | 建議 roughness | 亮面排序 | highlight_compactness | 高頻能量 |
|---|---:|---:|---:|---:|
| 頭髮 | 0.18 | 1（最亮） | 0.068 | 22.7 |
| 球衣印刷 | 0.22 | 2 | 0.026 | 19.1 |
| 皮膚塑膠 | 0.34 | 3 | 0.133 | 14.2 |
| 橡膠鞋 | 0.38 | 4 | 0.063 | 9.7 |
| 牛仔 | 0.52 | 5（最霧） | 0.075 | 12.9 |

`highlight_compactness` 只是觀測值，不用來排序 —— 它同時受部件本身形狀影響，
頭髮 0.068 與橡膠 0.063 的差距在雜訊內，算平手而不是先後。排序依據是
標記為 `ordinal` 的 roughness 判斷。

現行 [`brick_v2_spec.py:145-153`](./backend/brick_v2_spec.py#L145-L153) 只有一組全域
`base_roughness` 0.30 與 `print_roughness` 0.36。單一數值不可能重現這個分布，
這是目前渲染與參考圖之間**視覺差距最大的一項**。

roughness 欄位標記為 `ordinal`：排序可信，絕對值需要渲染端跑一次校正。

## 4. 印刷線寬 —— 「有些比較 2D」的真正來源

線寬與其他印刷特徵，一律由
[`backend/style_fingerprint.py`](./backend/style_fingerprint.py) 的
`panel_fingerprint` 量測，**參考圖與候選圖走同一段程式**。第一版曾用另一套
手寫量法定目標值，結果目標與候選不可比，band 沒有任何東西滿足得了。

量測必須在固定的標準解析度（256px）上做。同一張圖在 97px 與 512px 之間，
遮罩衍生的特徵會漂移超過 70%；固定解析度後降到 3%。

| 區域 | stroke_width_rel | mark_share | edge_density | local_contrast |
|---|---:|---:|---:|---:|
| 臉 | 0.050 | 0.154 | 0.036 | 14.1 |
| 軀幹服裝 | 0.058 | 0.329 | 0.030 | 15.4 |
| 牛仔腿 | 0.031 | 0.108 | 0.009 | 4.2 |
| 鞋 | 0.070 | 0.080 | 0.005 | 3.6 |

臉部 `stroke_width_rel` 0.050 換算 512px panel 約 **26 px**，容許範圍
±45%（約 14–37 px）。現行 [`style_family.py:33`](./backend/style_family.py#L33)
定義 `line_width_px: [4, 18]` —— **參考圖的線比規格允許的最粗值還粗。**
細線讀起來像插畫草稿，粗線才讀起來像玩具印刷，這是同一批生成裡
「有些比較 2D」最直接的成因。

素面褲子、素面上衣沒有印刷可量，線寬檢查會跳過（`mark_share` 低於 0.01），
不會把忠實的純色服裝判成風格違規。

## 5. 哪些特徵可以當閘門

| 轉移信心 | 意義 | 特徵 |
|---|---|---|
| `direct` | 跨解析度與跨媒介都成立，可設硬性門檻 | `directional_gradient`（誤差 5%）、`stroke_width_rel`（誤差 3%） |
| `indirect` | 候選之間可互比，但不可對參考圖設門檻 | `mark_share`、`edge_density`、`local_contrast`、`saturation_median` |

`indirect` 的理由：參考圖是渲染圖，帶有平面 UV 不會有的輪廓與遮蔽邊緣。
這些特徵單獨不判失敗，只記為觀測；它們真正的用途是**跨角色的離散度**——
`fingerprint_spread` 才是「這批角色是不是同一個物種」的量化定義，
而這正是整條管線之前完全沒有的東西。

第 6 節的渲染邊緣密度（臉 0.063、軀幹 0.037、牛仔腿 0.101、鞋 0.116）
保留為文件，不再用於判定。腿部密度全身最高，說明現行
[`brick_v2_spec.py:91-100`](./backend/brick_v2_spec.py#L91-L100)
每條腿 256×512 的貼圖預算偏低。

## 6. 調色盤

| 用途 | 色值 | 飽和度 (HSV S) | 備註 |
|---|---|---:|---|
| 背景 | `#FEFEFD` | 2 | 近純白 |
| 皮膚塑膠 | `#DFAB30` | 202 | 高飽和 |
| 頭髮塑膠 | `#2C2A26` | 30 | **暖調深灰，不是純黑** |
| 深色服裝（示例） | `#122B1E` | 148 | |
| 牛仔（示例） | `#7F8283` | 9 | 近中性 |
| 橡膠（示例） | `#DEDBD6` | 10 | |

服裝色是示例不是約束；皮膚與頭髮的**飽和度與明度區間**才是風格約束。

## 7. 比例骨架

垂直位置（佔主體總高的比例）：

| 位置 | 值 | 信心 |
|---|---:|---|
| 髮頂 | 0.000 | measured |
| 頭頂 | 0.096 | occluded（瀏海遮住頭頂） |
| 領口 | 0.327 | measured |
| 腰線 | 0.614 | measured |
| 褲襠 | 0.765 | measured |
| 雙腿分離 | 0.770 | measured |
| 鞋口 | 0.925 | measured |
| 鞋底 | 0.982 | measured |

寬度（佔主體總寬的比例）：

| 部位 | 值 | 信心 |
|---|---:|---|
| 頭 | 0.320 | occluded（頭髮蓋住兩側） |
| 頭髮最寬 | 0.578 | measured |
| 軀幹（不含手臂） | 0.591 | measured |
| 手臂展開 | 0.868 | measured |
| 含手 | 0.993 | measured |
| 單腿 | 0.287 | measured |
| 腿間隙 | 0.020 | measured |
| 雙鞋跨距 | 0.680 | measured |

**頭部比例不採用**：參考圖的長髮蓋住頭的兩側與頭頂，量到的 0.320 只是可見的
黃色區域，不是頭磚本身。要定頭部比例需要一張短髮或無髮的參考圖。
現階段沿用 `brick_v2_spec.py` 既有的頭部數值。

衍生比值：

| 比值 | 值 |
|---|---:|
| 軀幹高／總高 | 0.287 |
| 腿段高／總高 | 0.311 |
| 鞋高／總高 | 0.075 |
| 雙腿寬／軀幹寬 | 0.971 |
| 手臂展開／軀幹寬 | 1.469 |
| 雙鞋跨距／雙腿跨距 | 1.156 |

---

## 這份標準對現有規格的四項修正

| 項目 | 現況 | 參考圖量測 | 影響 |
|---|---|---|---|
| 貼圖方向性梯度門檻 | 0.17／0.20 | 應為 0.06 | 現行門檻讓雙重打光通過 |
| 粗糙度 | 單一全域值 | 五組材質分級 0.18–0.52 | 塑膠質感差距最大一項 |
| 臉部線寬 | 4–18 px | 512 panel 上約 26 px（14–37） | 「有些比較 2D」的直接成因 |
| 腿部貼圖 | 256×512 | 細節密度 0.101 為全身最高 | 預算偏低，撐不起業界級縫線與磨白 |

## 已接入生成管線的部分

| 模組 | 職責 |
|---|---|
| [`backend/style_normalizer.py`](./backend/style_normalizer.py) | 確定性壓平：移除平滑方向性光場，保留磨白等風化 |
| [`backend/style_fingerprint.py`](./backend/style_fingerprint.py) | 風格指紋量測、對基底標準的距離、跨角色離散度 |
| [`backend/ai_texture_gen.py`](./backend/ai_texture_gen.py) | 先壓平再判定；出貨的貼圖是壓平後的 panel |

流程是**先壓平、壓不動才判失敗**：可用程式移除的打光不值得付費重生，
壓平之後還超標的才是模型真的做錯。壓平是冪等的，乾淨的 panel 原封不動。

實作過程量到一件值得記錄的事：擬合是在模糊後的亮度上做二階曲面，
因此 panel 尺度的方向性明暗**幾乎都可移除**，包含硬切的雙色分割。
`unfixable` 是防禦路徑，不是常見結果。判失敗實務上主要來自線寬與
brick_v2 的服裝輪廓／背景檢查。

## 尚未由這份標準決定的事

- **頭部比例**：需要一張短髮參考圖才能量。
- **roughness 絕對值**：排序已定，數值需渲染端校正一次。
- **多套服裝**：目前只有一套（球衣＋牛仔＋球鞋）。裙裝、外套、帽 T 的印刷密度
  與風化語言仍需各自的參考圖。
- **皮膚色是否隨人變化**：參考圖是單一黃色塑膠。是否允許少量色階變化尚未決定，
  目前不寫入標準。
