# PersonaFlow Brick v2 可執行規格

## 目標

`brick_v2` 的目標不是「方盒模型貼上人物插畫」，而是接近高完成度商業積木玩具：圓角塑膠、梯形軀幹、收窄手臂、C形手、寬腿、獨立鞋型、立體髮片，以及真正貼合模型表面的臉部與服裝印刷。

AI只產生平面印刷，不得決定頭、髮型、軀幹、裙子、外套、手、腿或鞋的外輪廓。

## 不可妥協規則

- 不使用浮在模型前面的 `PlaneGeometry` 當作正式衣服或腿部材質。
- 不接受完整衣服輪廓、人物輪廓、衣服外背景或方向性陰影進入 UV。
- 臉部印刷使用貼合頭部弧度的曲面網格，不能是一張懸浮在頭前的矩形貼紙。
- 軀幹、左右腿與鞋使用模型正式 UV。
- C形手、梯形軀幹、圓角頭、收窄手臂、寬腿與鞋底屬於物種規格，所有人物共用。
- 高矮胖瘦只能縮放既有部件，範圍由 `backend/brick_v2_spec.py` 限制。
- AI材質、UV或WebGL任何一項失敗，整筆正式結果失敗，不建立替代角色。

## 角色比例

所有尺寸以相對場景單位定義，權威數值位於 `backend/brick_v2_spec.py`。

- 頭寬接近軀幹上緣寬度，並有一致倒角。
- 軀幹肩部較寬、腰部較窄，形成與目標積木人物一致的上寬下窄輪廓。
- 手臂由肩到腕逐漸收窄，預設只向外10度。
- 手使用具有開口與厚度的C形幾何，不使用球體。
- 腿較現行版本寬、短，鞋具有鞋面、鞋底與圓角鞋頭。

## UV與AI輸出

### 臉部

- AI輸出透明「ink-only」臉部印刷。
- 本機把印刷映射到只覆蓋頭部正面的弧形曲面；它與頭部同曲率，不會浮成平面貼紙。
- 五官只能位於正面安全區，側面與背面維持固定膚色。
- 頭髮不畫進臉部材質。

### 軀幹

- AI輸出必須從四邊延伸至四邊，整張都是軀幹表面材質。
- 允許領口、拉鍊、號碼、條紋與圖案。
- 禁止畫出洋裝、背心、外套或裙擺的完整外輪廓。
- 裙子、外套厚度與其他改變輪廓的服裝必須由3D部件處理。

### 腿與鞋

- 左右腿分開輸出，材質必須延伸到邊緣。
- 補丁、口袋、縫線可以印刷；腿外背景與整條褲管輪廓禁止。
- 鞋面印刷為選配，鞋型與鞋底永遠由固定幾何提供。

## 髮型候選架構

### A. 模組化參數化3D髮片（已選定）

使用髮帽、分線、瀏海、左右側髮、後髮、馬尾與髮髻模組組合，再以長度、波浪、捲度與蓬鬆度參數變形。少量模組即可產生大量組合，角色轉身時仍然成立。

第一版正式採用此策略；B、C只保留為後續研究，不進入目前正式資料流。

## 不同服裝與體型

角色不會只有一個完全固定的長方體，也不會讓AI任意生成新身體。正式架構分為四層：

1. **物種核心**：頭、C形手、腳、關節語言、材質與燈光固定。
2. **體型參數**：身高、肩寬、軀幹寬深與肢體粗細在規格範圍內縮放。
3. **服裝幾何文法**：無袖／短袖／長袖、外套／帽T／長大衣、褲／短褲／裙／洋裝使用有限且可組合的固定部件。
4. **AI印刷**：只負責顏色、領口、拉鍊、條紋、圖案、口袋與臉部特徵，不得改變輪廓。

`backend/brick_v2_garment.py` 將開放式衣物辨識結果轉成確定性的 `garment_geometry`。寬鬆衣服只改變 `fit` 與服裝外殼，不直接把人物判定為較胖；體型仍使用受限且帶信心值的相對估計。

### B. AI分層透明髮卡

AI從照片產生正面、側面與後方透明髮片，套在固定頭部周圍。正面最接近照片，開發較快，但轉身時仍可能看到紙片感與交叉面。

### C. 每人即時 image-to-3D 髮型

每位參與者生成獨立髮型網格。變化最大，但延遲、成本、拓樸、碰撞與多人效能最難控制，不建議作第一版正式管線。

## 開發閘門

在重新使用真人照片前，必須先以合成棋盤、條紋、臉部與服裝測試圖完成：

1. 正面、側面與45度視角都沒有浮動貼紙。
2. 頭部印刷隨圓柱表面彎曲。
3. 軀幹、腿與鞋沒有拉伸、越界或接縫。
4. C形手、梯形軀幹、寬腿與鞋型可一眼辨識為同一積木物種。
5. Preview與群體投影使用同一份幾何建構器或同一GLB。
6. Atlas Validator能拒絕目前P01的完整洋裝輪廓與粉紅／灰色背景污染。

通過上述閘門後才進入 `USER_MANUAL_GENERATION_REQUIRED`，重新測試P01。

## 開發切換

公開流程目前仍預設 `brick_v1`。完成合成頁與髮型選型後，可在 `.env` 設定：

```env
BRICK_CHARACTER_STYLE=brick_v2
```

重啟後端後，`GET /health` 的 `character_style` 必須顯示 `brick_v2`；完整規格可由
`GET /api/styles/brick_v2` 查詢。切換本身不會呼叫AI，只有按下正式生成並送入真人照片才會產生API成本。
