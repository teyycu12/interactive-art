# 2026q3_owner_curated — 來源與授權

> 在「允許重製散布」與「允許公開展示產出」兩項風險排除之前，
> 這組圖不得用於公開展示，也不得推送到公開 repo。
> 這兩項出自素材採用前的授權查核標準：**(a)** 模型／素材的商用授權限制為何，
> **(b)** 公開展示產出是否為一般授權即涵蓋，還是另需個別授權。
> 目前這兩項的判定仍是「不行」／「未完全排除」，見下方〈授權狀態〉與〈目前結論〉。

## 這組是什麼

5 張全身參考圖，作為 `full_character` 生成時的 **Image 4（風格 sheet）**。

**這不是訓練資料。** 沒有任何權重被更新。這些圖是 **prompt-time 條件輸入**：
`backend/garment_gen.py` 的 `_build_style_reference_sheet()` 每次生成時把它們拼成一張
1024×1536 的 sheet，連同訪客照片一起送往 OpenRouter → Google。也就是說，
**每一次生成都會把這 5 張圖傳輸給第三方服務**。

檔案是執行期資產：沒有它們，`_style_reference_sheet()` 會印出
`not found, sending without it` 並退回三張圖的送法，生成不會失敗，但物種統一效果會消失。

## 來源

| 項目 | 內容 |
|---|---|
| 取得方式 | 自行以 AI 生圖工具生成，Elser（Elser AI，動漫／OC 生成平台），免費帳號 |
| 來源 URL 或工具 | https://www.elser.ai/zh-TW |
| 取得日期 | 2026-08-22 |
| 原始檔規格 | 1344×768 WebP，橫式，EXIF/XMP 皆已無資料 |
| 出貨檔規格 | 576×864 PNG，直式單人 |
| 加工 | 裁切為直式單人構圖、**移除浮水印** |
| 原始檔保留位置 | `_original_watermarked/`（帶浮水印，5 張） |

> 檔案本身查不到任何來源線索——metadata 是空的。這一欄只有專案擁有者能填。

## 授權狀態

> 以下條款內容來自 2026-08-23 用網頁擷取工具讀取 Elser Terms of Service
> （https://www.elser.ai/terms-of-service），**不是人工逐字核對原文**，
> 屬於二手摘要。推上公開 repo 前，請自己重讀一次原文確認沒有誤讀。

| 權利 | 狀態 | 影響 |
|---|---|---|
| 授權條款名稱／URL | Elser AI Terms of Service — https://www.elser.ai/terms-of-service | — |
| 允許商業使用 | **是**（條款 4.4：「does not restrict the use of Outputs for commercial use」）。但同條款也授予 Elser 對 Input/Output 的永久、不可撤銷、免權利金授權（含訓練 AI 模型與行銷用途），是雙向授權，不是使用者獨佔 | 展覽若涉商業性質，此項風險較低 |
| **允許重製散布** | **未明確授權，判定為不行**。條款 5.2(i) 禁止使用者「license, sell, rent, lease, transfer, assign, reproduce, distribute, host」，文字對象是「the Service」（平台本身），不是逐字針對 Output，但也沒查到針對 Output 重製散布的明文許可條款。加上這組圖**原始檔帶浮水印、出貨檔已移除**，浮水印是平台權利主張最明確的形式，兩者疊加使曝險升高 | **決定能不能推上 git 讓人下載——目前判定為不行** |
| 允許公開展示產出 | 未找到明文禁止，商業使用條款隱含可公開展示，但沒有明文保障，且去浮水印的曝險同上仍在 | 決定能不能現場展出——建議展出前先解決浮水印／重製散布的不確定性 |
| 允許傳輸給第三方 AI 服務 | 條款沒規範使用者把 Output 再傳給其他第三方服務；3.4 條要求使用者自行確保不侵犯第三方權利，風險由使用者承擔 | 每次生成都會發生，見上 |
| 要求標示出處 | 沒查到明文要求 | — |

**浮水印是權利主張最明確的形式。** 原始檔帶浮水印、出貨檔已移除，
「去浮水印 + 公開散布」是曝險最高的組合。這份文件只是誠實記錄，
**不會讓散布變得合法**——若條款不允許散布，正確的動作是不要推，不是補文件。

**目前結論**：你計畫的兩個用途裡，「發布到 GitHub」屬於重製散布，依上表**目前不建議執行**；
「展覽現場展示」風險較低但仍未完全排除，建議展前處理完浮水印／散布的不確定性再決定。

## 已知品質限制

兩項在 `docs/STYLE_PROBE_FOLLOWUPS.md` 已記錄的實測問題，若日後重生這組請一併修正：

1. **5 張裡有 4 張雙腿併攏成實心柱**，只在鞋子附近（約 96% 高度）才分開。
   這與 prompt 自己的 `MANDATORY LEGO DESIGN RULES #1`（雙腿間需有可見垂直間隙）
   及 `_canonical_lego_pose()` 畫的 24px 間隙直接衝突，也使分部位身高縮放
   在幾何上量不到 inseam。`full_body_03`（一體式洋裝）正當豁免。
2. **5 張髮色全落在 hue 16°–32°、saturation ≥ 92** 的窄帶。`_STYLE_SHEET_FIREWALL`
   雖明文禁止髮色轉移，但單一色帶重複 5 次是否造成偏壓，需要用
   `STYLE_REFERENCE_MODE=off` 做 A/B 實測，不能用推論的。

## 下載者須知

這些圖是本專案的執行期資產，**不是可自由再利用的素材**。
來源是 Elser AI（免費帳號）生成，商業使用條款上是允許的，但「重製散布」未查到明文授權，
且出貨檔已移除原始浮水印，因此**不要把這組圖（或帶有它們的 repo）推上公開 git**，
也不要另外轉散布，直到這一項被更明確地確認過。

## 若查不出來源

最乾淨的解法不是把這份文件寫模糊，而是**重生一組**：用有明確授權的工具自己產 5 張，
來源即為「我在 X 平台以 Y 帳號於 Z 日生成」，散布權利清楚，
並可順手修掉上面兩項品質限制。
